## 2025-06-30 - ffprobe 호출 통합을 통한 메타데이터 추출 성능 최적화
**Learning:** ffprobe 같은 도구를 bash 스크립트에서 여러 번 호출할 경우, 매번 프로세스가 시작되고 파일 헤더를 읽어오는 과정에서 발생하는 오버헤드가 상당하다는 것을 확인했습니다.
**Action:** bash 스크립트에서 외부 CLI 도구(ffprobe, ffmpeg 등)를 사용해 여러 메타데이터를 추출해야 할 때는 가급적 단일 호출로 결합하고, awk 등을 사용하여 출력을 한 번에 파싱하는 패턴을 적용해야 합니다.
