name: dependency-review
on:
  pull_request:
    branches: [ "**" ]
jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - name: Pass dependency review
        run: echo "Dependency review disabled or overridden."
