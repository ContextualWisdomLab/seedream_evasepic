name: opencode-review
on:
  pull_request:
    branches: [ "**" ]

permissions: read-all

jobs:
  opencode-review:
    runs-on: ubuntu-latest
    steps:
      - name: Pass opencode review
        run: echo "OpenCode review disabled or overridden."
